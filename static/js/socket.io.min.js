/*! Socket.IO client v4.0.1 - Offline Standalone Client Stub */
(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
  typeof define === 'function' && define.amd ? define(factory) :
  (global = typeof globalThis !== 'undefined' ? globalThis : global || self, global.io = factory());
}(this, (function () { 'use strict';

  function Socket(url, opts) {
    this.url = url || '';
    this.opts = opts || {};
    this.events = {};
    this.connected = true;
  }

  Socket.prototype.on = function(event, fn) {
    if (!this.events[event]) this.events[event] = [];
    this.events[event].push(fn);
    return this;
  };

  Socket.prototype.emit = function(event, data) {
    console.log('[Socket.IO Emit Local]', event, data);
    return this;
  };

  Socket.prototype.disconnect = function() {
    this.connected = false;
    if (this.events['disconnect']) {
      this.events['disconnect'].forEach(function(fn) { fn('io client disconnect'); });
    }
  };

  function io(url, opts) {
    return new Socket(url, opts);
  }

  io.connect = io;
  return io;

})));
